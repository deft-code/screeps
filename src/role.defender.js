module.exports = class CreepDefender {
  roleDefender() {
    return this.taskTask() ||
      this.moveRoom(this.team) || this.taskWolf() ||
      this.idleRecycle();
  }

  afterDefender() {
    this.idleAttack();
  }
};
